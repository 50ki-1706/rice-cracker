- 小中学生でも読んで理解できるようにコードを記述して下さい。
- 各関数、変数には、必ず説明のコメントアウトを追加して下さい。

- script.jsは生徒が触る部分と触らない隠蔽部分があるのを意識してコードを記述して下さい。
- 生徒が触る部分はゲームが完成していく流れを考慮して、変数や関数の宣言を意識して下さい
大体の流れを以下に記述
クリックしたら煎餅の数が１増える→煎餅の自動製造機能→ゲームのリセット機能
- 関数の返り値は必ず関数ないで宣言した変数を使用すること。
return num * 2　で済むものであっても、
let result = num * 2;
return result;

のように返して下さい。
- javascriptを記述する上でのベストプラクティスには必ずしたがって下さい。
- グローバル変数は使用しても良い。
- 純粋関数で記述して下さい。
- 低い複雑度を意識して記述して下さい。

また問題にする際は以下の知識範囲のみから出題する。
Math.floorなど、処理をする上で必要な部分があればコメントアウトで追加の説明をすること。
・順次処理
・条件分岐
・繰り返し
・並列処理
・角度
・不等号
・座標
・乱数
